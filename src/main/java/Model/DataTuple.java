package Model;

/**
 * Let DataTuple be a 2-tuple DT such that:
 *      DT is a 2-tuple (x, y) where
 *      x is a n-dimensional array with 50,000 entries. Where each entry is another n-dimensional array with 784 values.
 *          This represents the the 784 pixels in each image.
 *      y is an n-dimensional array with with 50,000 entries. Where each entry is a single digit range(0-9)
 *
 */
public class DataTuple {

}
